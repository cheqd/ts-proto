"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: cheqd/oracle/v2/query.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueryClientImpl = exports.QueryServiceName = exports.ConvertUSDCtoCHEQResponse = exports.ConvertUSDCtoCHEQRequest = exports.QuerySMAResponse = exports.QuerySMARequest = exports.QueryWMAResponse = exports.QueryWMARequest = exports.QueryEMAResponse = exports.QueryEMARequest = exports.QueryValidatorRewardSetResponse = exports.QueryValidatorRewardSet = exports.QueryMedianDeviationsResponse = exports.QueryMedianDeviations = exports.QueryMediansResponse = exports.QueryMedians = exports.QueryParamsResponse = exports.QueryParams = exports.QueryAggregateVotesResponse = exports.QueryAggregateVotes = exports.QueryAggregateVoteResponse = exports.QueryAggregateVote = exports.QueryAggregatePrevotesResponse = exports.QueryAggregatePrevotes = exports.QueryAggregatePrevoteResponse = exports.QueryAggregatePrevote = exports.QuerySlashWindowResponse = exports.QuerySlashWindow = exports.QueryMissCounterResponse = exports.QueryMissCounter = exports.QueryFeederDelegationResponse = exports.QueryFeederDelegation = exports.QueryActiveExchangeRatesResponse = exports.QueryActiveExchangeRates = exports.QueryExchangeRatesResponse = exports.QueryExchangeRates = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const coin_js_1 = require("../../../cosmos/base/v1beta1/coin.js");
const oracle_js_1 = require("./oracle.js");
function createBaseQueryExchangeRates() {
    return { denom: "" };
}
exports.QueryExchangeRates = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryExchangeRates();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { denom: isSet(object.denom) ? globalThis.String(object.denom) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        return obj;
    },
    create(base) {
        return exports.QueryExchangeRates.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryExchangeRates();
        message.denom = object.denom ?? "";
        return message;
    },
};
function createBaseQueryExchangeRatesResponse() {
    return { exchangeRates: [] };
}
exports.QueryExchangeRatesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.exchangeRates) {
            coin_js_1.DecCoin.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryExchangeRatesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.exchangeRates.push(coin_js_1.DecCoin.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            exchangeRates: globalThis.Array.isArray(object?.exchangeRates)
                ? object.exchangeRates.map((e) => coin_js_1.DecCoin.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.exchangeRates?.length) {
            obj.exchangeRates = message.exchangeRates.map((e) => coin_js_1.DecCoin.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.QueryExchangeRatesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryExchangeRatesResponse();
        message.exchangeRates = object.exchangeRates?.map((e) => coin_js_1.DecCoin.fromPartial(e)) || [];
        return message;
    },
};
function createBaseQueryActiveExchangeRates() {
    return {};
}
exports.QueryActiveExchangeRates = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryActiveExchangeRates();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QueryActiveExchangeRates.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQueryActiveExchangeRates();
        return message;
    },
};
function createBaseQueryActiveExchangeRatesResponse() {
    return { activeRates: [] };
}
exports.QueryActiveExchangeRatesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.activeRates) {
            writer.uint32(10).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryActiveExchangeRatesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.activeRates.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            activeRates: globalThis.Array.isArray(object?.activeRates)
                ? object.activeRates.map((e) => globalThis.String(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.activeRates?.length) {
            obj.activeRates = message.activeRates;
        }
        return obj;
    },
    create(base) {
        return exports.QueryActiveExchangeRatesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryActiveExchangeRatesResponse();
        message.activeRates = object.activeRates?.map((e) => e) || [];
        return message;
    },
};
function createBaseQueryFeederDelegation() {
    return { validatorAddr: "" };
}
exports.QueryFeederDelegation = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.validatorAddr !== "") {
            writer.uint32(10).string(message.validatorAddr);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryFeederDelegation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.validatorAddr = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { validatorAddr: isSet(object.validatorAddr) ? globalThis.String(object.validatorAddr) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddr !== "") {
            obj.validatorAddr = message.validatorAddr;
        }
        return obj;
    },
    create(base) {
        return exports.QueryFeederDelegation.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryFeederDelegation();
        message.validatorAddr = object.validatorAddr ?? "";
        return message;
    },
};
function createBaseQueryFeederDelegationResponse() {
    return { feederAddr: "" };
}
exports.QueryFeederDelegationResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.feederAddr !== "") {
            writer.uint32(10).string(message.feederAddr);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryFeederDelegationResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.feederAddr = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { feederAddr: isSet(object.feederAddr) ? globalThis.String(object.feederAddr) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.feederAddr !== "") {
            obj.feederAddr = message.feederAddr;
        }
        return obj;
    },
    create(base) {
        return exports.QueryFeederDelegationResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryFeederDelegationResponse();
        message.feederAddr = object.feederAddr ?? "";
        return message;
    },
};
function createBaseQueryMissCounter() {
    return { validatorAddr: "" };
}
exports.QueryMissCounter = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.validatorAddr !== "") {
            writer.uint32(10).string(message.validatorAddr);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMissCounter();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.validatorAddr = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { validatorAddr: isSet(object.validatorAddr) ? globalThis.String(object.validatorAddr) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddr !== "") {
            obj.validatorAddr = message.validatorAddr;
        }
        return obj;
    },
    create(base) {
        return exports.QueryMissCounter.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMissCounter();
        message.validatorAddr = object.validatorAddr ?? "";
        return message;
    },
};
function createBaseQueryMissCounterResponse() {
    return { missCounter: 0n };
}
exports.QueryMissCounterResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.missCounter !== 0n) {
            if (BigInt.asUintN(64, message.missCounter) !== message.missCounter) {
                throw new globalThis.Error("value provided for field message.missCounter of type uint64 too large");
            }
            writer.uint32(8).uint64(message.missCounter);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMissCounterResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.missCounter = reader.uint64();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { missCounter: isSet(object.missCounter) ? BigInt(object.missCounter) : 0n };
    },
    toJSON(message) {
        const obj = {};
        if (message.missCounter !== 0n) {
            obj.missCounter = message.missCounter.toString();
        }
        return obj;
    },
    create(base) {
        return exports.QueryMissCounterResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMissCounterResponse();
        message.missCounter = object.missCounter ?? 0n;
        return message;
    },
};
function createBaseQuerySlashWindow() {
    return {};
}
exports.QuerySlashWindow = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQuerySlashWindow();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QuerySlashWindow.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQuerySlashWindow();
        return message;
    },
};
function createBaseQuerySlashWindowResponse() {
    return { windowProgress: 0n };
}
exports.QuerySlashWindowResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.windowProgress !== 0n) {
            if (BigInt.asUintN(64, message.windowProgress) !== message.windowProgress) {
                throw new globalThis.Error("value provided for field message.windowProgress of type uint64 too large");
            }
            writer.uint32(8).uint64(message.windowProgress);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQuerySlashWindowResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.windowProgress = reader.uint64();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { windowProgress: isSet(object.windowProgress) ? BigInt(object.windowProgress) : 0n };
    },
    toJSON(message) {
        const obj = {};
        if (message.windowProgress !== 0n) {
            obj.windowProgress = message.windowProgress.toString();
        }
        return obj;
    },
    create(base) {
        return exports.QuerySlashWindowResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQuerySlashWindowResponse();
        message.windowProgress = object.windowProgress ?? 0n;
        return message;
    },
};
function createBaseQueryAggregatePrevote() {
    return { validatorAddr: "" };
}
exports.QueryAggregatePrevote = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.validatorAddr !== "") {
            writer.uint32(10).string(message.validatorAddr);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregatePrevote();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.validatorAddr = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { validatorAddr: isSet(object.validatorAddr) ? globalThis.String(object.validatorAddr) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddr !== "") {
            obj.validatorAddr = message.validatorAddr;
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregatePrevote.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregatePrevote();
        message.validatorAddr = object.validatorAddr ?? "";
        return message;
    },
};
function createBaseQueryAggregatePrevoteResponse() {
    return { aggregatePrevote: undefined };
}
exports.QueryAggregatePrevoteResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.aggregatePrevote !== undefined) {
            oracle_js_1.AggregateExchangeRatePrevote.encode(message.aggregatePrevote, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregatePrevoteResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.aggregatePrevote = oracle_js_1.AggregateExchangeRatePrevote.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            aggregatePrevote: isSet(object.aggregatePrevote)
                ? oracle_js_1.AggregateExchangeRatePrevote.fromJSON(object.aggregatePrevote)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.aggregatePrevote !== undefined) {
            obj.aggregatePrevote = oracle_js_1.AggregateExchangeRatePrevote.toJSON(message.aggregatePrevote);
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregatePrevoteResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregatePrevoteResponse();
        message.aggregatePrevote = (object.aggregatePrevote !== undefined && object.aggregatePrevote !== null)
            ? oracle_js_1.AggregateExchangeRatePrevote.fromPartial(object.aggregatePrevote)
            : undefined;
        return message;
    },
};
function createBaseQueryAggregatePrevotes() {
    return {};
}
exports.QueryAggregatePrevotes = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregatePrevotes();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QueryAggregatePrevotes.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQueryAggregatePrevotes();
        return message;
    },
};
function createBaseQueryAggregatePrevotesResponse() {
    return { aggregatePrevotes: [] };
}
exports.QueryAggregatePrevotesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.aggregatePrevotes) {
            oracle_js_1.AggregateExchangeRatePrevote.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregatePrevotesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.aggregatePrevotes.push(oracle_js_1.AggregateExchangeRatePrevote.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            aggregatePrevotes: globalThis.Array.isArray(object?.aggregatePrevotes)
                ? object.aggregatePrevotes.map((e) => oracle_js_1.AggregateExchangeRatePrevote.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.aggregatePrevotes?.length) {
            obj.aggregatePrevotes = message.aggregatePrevotes.map((e) => oracle_js_1.AggregateExchangeRatePrevote.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregatePrevotesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregatePrevotesResponse();
        message.aggregatePrevotes = object.aggregatePrevotes?.map((e) => oracle_js_1.AggregateExchangeRatePrevote.fromPartial(e)) || [];
        return message;
    },
};
function createBaseQueryAggregateVote() {
    return { validatorAddr: "" };
}
exports.QueryAggregateVote = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.validatorAddr !== "") {
            writer.uint32(10).string(message.validatorAddr);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregateVote();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.validatorAddr = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { validatorAddr: isSet(object.validatorAddr) ? globalThis.String(object.validatorAddr) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddr !== "") {
            obj.validatorAddr = message.validatorAddr;
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregateVote.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregateVote();
        message.validatorAddr = object.validatorAddr ?? "";
        return message;
    },
};
function createBaseQueryAggregateVoteResponse() {
    return { aggregateVote: undefined };
}
exports.QueryAggregateVoteResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.aggregateVote !== undefined) {
            oracle_js_1.AggregateExchangeRateVote.encode(message.aggregateVote, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregateVoteResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.aggregateVote = oracle_js_1.AggregateExchangeRateVote.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            aggregateVote: isSet(object.aggregateVote) ? oracle_js_1.AggregateExchangeRateVote.fromJSON(object.aggregateVote) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.aggregateVote !== undefined) {
            obj.aggregateVote = oracle_js_1.AggregateExchangeRateVote.toJSON(message.aggregateVote);
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregateVoteResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregateVoteResponse();
        message.aggregateVote = (object.aggregateVote !== undefined && object.aggregateVote !== null)
            ? oracle_js_1.AggregateExchangeRateVote.fromPartial(object.aggregateVote)
            : undefined;
        return message;
    },
};
function createBaseQueryAggregateVotes() {
    return {};
}
exports.QueryAggregateVotes = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregateVotes();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QueryAggregateVotes.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQueryAggregateVotes();
        return message;
    },
};
function createBaseQueryAggregateVotesResponse() {
    return { aggregateVotes: [] };
}
exports.QueryAggregateVotesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.aggregateVotes) {
            oracle_js_1.AggregateExchangeRateVote.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryAggregateVotesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.aggregateVotes.push(oracle_js_1.AggregateExchangeRateVote.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            aggregateVotes: globalThis.Array.isArray(object?.aggregateVotes)
                ? object.aggregateVotes.map((e) => oracle_js_1.AggregateExchangeRateVote.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.aggregateVotes?.length) {
            obj.aggregateVotes = message.aggregateVotes.map((e) => oracle_js_1.AggregateExchangeRateVote.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.QueryAggregateVotesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryAggregateVotesResponse();
        message.aggregateVotes = object.aggregateVotes?.map((e) => oracle_js_1.AggregateExchangeRateVote.fromPartial(e)) || [];
        return message;
    },
};
function createBaseQueryParams() {
    return {};
}
exports.QueryParams = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryParams();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QueryParams.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQueryParams();
        return message;
    },
};
function createBaseQueryParamsResponse() {
    return { params: undefined };
}
exports.QueryParamsResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.params !== undefined) {
            oracle_js_1.Params.encode(message.params, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryParamsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.params = oracle_js_1.Params.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { params: isSet(object.params) ? oracle_js_1.Params.fromJSON(object.params) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.params !== undefined) {
            obj.params = oracle_js_1.Params.toJSON(message.params);
        }
        return obj;
    },
    create(base) {
        return exports.QueryParamsResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryParamsResponse();
        message.params = (object.params !== undefined && object.params !== null)
            ? oracle_js_1.Params.fromPartial(object.params)
            : undefined;
        return message;
    },
};
function createBaseQueryMedians() {
    return { denom: "", numStamps: 0 };
}
exports.QueryMedians = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        if (message.numStamps !== 0) {
            writer.uint32(16).uint32(message.numStamps);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMedians();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.numStamps = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
            numStamps: isSet(object.numStamps) ? globalThis.Number(object.numStamps) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        if (message.numStamps !== 0) {
            obj.numStamps = Math.round(message.numStamps);
        }
        return obj;
    },
    create(base) {
        return exports.QueryMedians.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMedians();
        message.denom = object.denom ?? "";
        message.numStamps = object.numStamps ?? 0;
        return message;
    },
};
function createBaseQueryMediansResponse() {
    return { medians: [] };
}
exports.QueryMediansResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.medians) {
            oracle_js_1.PriceStamp.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMediansResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.medians.push(oracle_js_1.PriceStamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            medians: globalThis.Array.isArray(object?.medians) ? object.medians.map((e) => oracle_js_1.PriceStamp.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.medians?.length) {
            obj.medians = message.medians.map((e) => oracle_js_1.PriceStamp.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.QueryMediansResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMediansResponse();
        message.medians = object.medians?.map((e) => oracle_js_1.PriceStamp.fromPartial(e)) || [];
        return message;
    },
};
function createBaseQueryMedianDeviations() {
    return { denom: "" };
}
exports.QueryMedianDeviations = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMedianDeviations();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { denom: isSet(object.denom) ? globalThis.String(object.denom) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        return obj;
    },
    create(base) {
        return exports.QueryMedianDeviations.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMedianDeviations();
        message.denom = object.denom ?? "";
        return message;
    },
};
function createBaseQueryMedianDeviationsResponse() {
    return { medianDeviations: [] };
}
exports.QueryMedianDeviationsResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.medianDeviations) {
            oracle_js_1.PriceStamp.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryMedianDeviationsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.medianDeviations.push(oracle_js_1.PriceStamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            medianDeviations: globalThis.Array.isArray(object?.medianDeviations)
                ? object.medianDeviations.map((e) => oracle_js_1.PriceStamp.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.medianDeviations?.length) {
            obj.medianDeviations = message.medianDeviations.map((e) => oracle_js_1.PriceStamp.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.QueryMedianDeviationsResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryMedianDeviationsResponse();
        message.medianDeviations = object.medianDeviations?.map((e) => oracle_js_1.PriceStamp.fromPartial(e)) || [];
        return message;
    },
};
function createBaseQueryValidatorRewardSet() {
    return {};
}
exports.QueryValidatorRewardSet = {
    encode(_, writer = new wire_1.BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryValidatorRewardSet();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return exports.QueryValidatorRewardSet.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseQueryValidatorRewardSet();
        return message;
    },
};
function createBaseQueryValidatorRewardSetResponse() {
    return { validators: undefined };
}
exports.QueryValidatorRewardSetResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.validators !== undefined) {
            oracle_js_1.ValidatorRewardSet.encode(message.validators, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryValidatorRewardSetResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.validators = oracle_js_1.ValidatorRewardSet.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { validators: isSet(object.validators) ? oracle_js_1.ValidatorRewardSet.fromJSON(object.validators) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.validators !== undefined) {
            obj.validators = oracle_js_1.ValidatorRewardSet.toJSON(message.validators);
        }
        return obj;
    },
    create(base) {
        return exports.QueryValidatorRewardSetResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryValidatorRewardSetResponse();
        message.validators = (object.validators !== undefined && object.validators !== null)
            ? oracle_js_1.ValidatorRewardSet.fromPartial(object.validators)
            : undefined;
        return message;
    },
};
function createBaseQueryEMARequest() {
    return { denom: "" };
}
exports.QueryEMARequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryEMARequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { denom: isSet(object.denom) ? globalThis.String(object.denom) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        return obj;
    },
    create(base) {
        return exports.QueryEMARequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryEMARequest();
        message.denom = object.denom ?? "";
        return message;
    },
};
function createBaseQueryEMAResponse() {
    return { price: "" };
}
exports.QueryEMAResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.price !== "") {
            writer.uint32(10).string(message.price);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryEMAResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.price = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { price: isSet(object.price) ? globalThis.String(object.price) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.price !== "") {
            obj.price = message.price;
        }
        return obj;
    },
    create(base) {
        return exports.QueryEMAResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryEMAResponse();
        message.price = object.price ?? "";
        return message;
    },
};
function createBaseQueryWMARequest() {
    return { denom: "", strategy: "", customWeights: [] };
}
exports.QueryWMARequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        if (message.strategy !== "") {
            writer.uint32(18).string(message.strategy);
        }
        writer.uint32(26).fork();
        for (const v of message.customWeights) {
            if (BigInt.asIntN(64, v) !== v) {
                throw new globalThis.Error("a value provided in array field customWeights of type int64 is too large");
            }
            writer.int64(v);
        }
        writer.join();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryWMARequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.strategy = reader.string();
                    continue;
                }
                case 3: {
                    if (tag === 24) {
                        message.customWeights.push(reader.int64());
                        continue;
                    }
                    if (tag === 26) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.customWeights.push(reader.int64());
                        }
                        continue;
                    }
                    break;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
            strategy: isSet(object.strategy) ? globalThis.String(object.strategy) : "",
            customWeights: globalThis.Array.isArray(object?.customWeights)
                ? object.customWeights.map((e) => BigInt(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        if (message.strategy !== "") {
            obj.strategy = message.strategy;
        }
        if (message.customWeights?.length) {
            obj.customWeights = message.customWeights.map((e) => e.toString());
        }
        return obj;
    },
    create(base) {
        return exports.QueryWMARequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryWMARequest();
        message.denom = object.denom ?? "";
        message.strategy = object.strategy ?? "";
        message.customWeights = object.customWeights?.map((e) => e) || [];
        return message;
    },
};
function createBaseQueryWMAResponse() {
    return { price: "" };
}
exports.QueryWMAResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.price !== "") {
            writer.uint32(10).string(message.price);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQueryWMAResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.price = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { price: isSet(object.price) ? globalThis.String(object.price) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.price !== "") {
            obj.price = message.price;
        }
        return obj;
    },
    create(base) {
        return exports.QueryWMAResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQueryWMAResponse();
        message.price = object.price ?? "";
        return message;
    },
};
function createBaseQuerySMARequest() {
    return { denom: "" };
}
exports.QuerySMARequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.denom !== "") {
            writer.uint32(10).string(message.denom);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQuerySMARequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.denom = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { denom: isSet(object.denom) ? globalThis.String(object.denom) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.denom !== "") {
            obj.denom = message.denom;
        }
        return obj;
    },
    create(base) {
        return exports.QuerySMARequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQuerySMARequest();
        message.denom = object.denom ?? "";
        return message;
    },
};
function createBaseQuerySMAResponse() {
    return { price: "" };
}
exports.QuerySMAResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.price !== "") {
            writer.uint32(10).string(message.price);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseQuerySMAResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.price = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { price: isSet(object.price) ? globalThis.String(object.price) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.price !== "") {
            obj.price = message.price;
        }
        return obj;
    },
    create(base) {
        return exports.QuerySMAResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseQuerySMAResponse();
        message.price = object.price ?? "";
        return message;
    },
};
function createBaseConvertUSDCtoCHEQRequest() {
    return { amount: "", maType: "", wmaStrategy: "", customWeights: [] };
}
exports.ConvertUSDCtoCHEQRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.amount !== "") {
            writer.uint32(10).string(message.amount);
        }
        if (message.maType !== "") {
            writer.uint32(18).string(message.maType);
        }
        if (message.wmaStrategy !== "") {
            writer.uint32(26).string(message.wmaStrategy);
        }
        writer.uint32(34).fork();
        for (const v of message.customWeights) {
            writer.int32(v);
        }
        writer.join();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseConvertUSDCtoCHEQRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.amount = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.maType = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.wmaStrategy = reader.string();
                    continue;
                }
                case 4: {
                    if (tag === 32) {
                        message.customWeights.push(reader.int32());
                        continue;
                    }
                    if (tag === 34) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.customWeights.push(reader.int32());
                        }
                        continue;
                    }
                    break;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
            maType: isSet(object.maType) ? globalThis.String(object.maType) : "",
            wmaStrategy: isSet(object.wmaStrategy) ? globalThis.String(object.wmaStrategy) : "",
            customWeights: globalThis.Array.isArray(object?.customWeights)
                ? object.customWeights.map((e) => globalThis.Number(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.amount !== "") {
            obj.amount = message.amount;
        }
        if (message.maType !== "") {
            obj.maType = message.maType;
        }
        if (message.wmaStrategy !== "") {
            obj.wmaStrategy = message.wmaStrategy;
        }
        if (message.customWeights?.length) {
            obj.customWeights = message.customWeights.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return exports.ConvertUSDCtoCHEQRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseConvertUSDCtoCHEQRequest();
        message.amount = object.amount ?? "";
        message.maType = object.maType ?? "";
        message.wmaStrategy = object.wmaStrategy ?? "";
        message.customWeights = object.customWeights?.map((e) => e) || [];
        return message;
    },
};
function createBaseConvertUSDCtoCHEQResponse() {
    return { amount: "" };
}
exports.ConvertUSDCtoCHEQResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.amount !== "") {
            writer.uint32(10).string(message.amount);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseConvertUSDCtoCHEQResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.amount = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { amount: isSet(object.amount) ? globalThis.String(object.amount) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.amount !== "") {
            obj.amount = message.amount;
        }
        return obj;
    },
    create(base) {
        return exports.ConvertUSDCtoCHEQResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseConvertUSDCtoCHEQResponse();
        message.amount = object.amount ?? "";
        return message;
    },
};
exports.QueryServiceName = "cheqd.oracle.v2.Query";
class QueryClientImpl {
    rpc;
    service;
    constructor(rpc, opts) {
        this.service = opts?.service || exports.QueryServiceName;
        this.rpc = rpc;
        this.ExchangeRates = this.ExchangeRates.bind(this);
        this.ActiveExchangeRates = this.ActiveExchangeRates.bind(this);
        this.FeederDelegation = this.FeederDelegation.bind(this);
        this.MissCounter = this.MissCounter.bind(this);
        this.SlashWindow = this.SlashWindow.bind(this);
        this.AggregatePrevote = this.AggregatePrevote.bind(this);
        this.AggregatePrevotes = this.AggregatePrevotes.bind(this);
        this.AggregateVote = this.AggregateVote.bind(this);
        this.AggregateVotes = this.AggregateVotes.bind(this);
        this.Params = this.Params.bind(this);
        this.Medians = this.Medians.bind(this);
        this.MedianDeviations = this.MedianDeviations.bind(this);
        this.ValidatorRewardSet = this.ValidatorRewardSet.bind(this);
        this.EMA = this.EMA.bind(this);
        this.WMA = this.WMA.bind(this);
        this.SMA = this.SMA.bind(this);
        this.ConvertUSDCtoCHEQ = this.ConvertUSDCtoCHEQ.bind(this);
    }
    ExchangeRates(request) {
        const data = exports.QueryExchangeRates.encode(request).finish();
        const promise = this.rpc.request(this.service, "ExchangeRates", data);
        return promise.then((data) => exports.QueryExchangeRatesResponse.decode(new wire_1.BinaryReader(data)));
    }
    ActiveExchangeRates(request) {
        const data = exports.QueryActiveExchangeRates.encode(request).finish();
        const promise = this.rpc.request(this.service, "ActiveExchangeRates", data);
        return promise.then((data) => exports.QueryActiveExchangeRatesResponse.decode(new wire_1.BinaryReader(data)));
    }
    FeederDelegation(request) {
        const data = exports.QueryFeederDelegation.encode(request).finish();
        const promise = this.rpc.request(this.service, "FeederDelegation", data);
        return promise.then((data) => exports.QueryFeederDelegationResponse.decode(new wire_1.BinaryReader(data)));
    }
    MissCounter(request) {
        const data = exports.QueryMissCounter.encode(request).finish();
        const promise = this.rpc.request(this.service, "MissCounter", data);
        return promise.then((data) => exports.QueryMissCounterResponse.decode(new wire_1.BinaryReader(data)));
    }
    SlashWindow(request) {
        const data = exports.QuerySlashWindow.encode(request).finish();
        const promise = this.rpc.request(this.service, "SlashWindow", data);
        return promise.then((data) => exports.QuerySlashWindowResponse.decode(new wire_1.BinaryReader(data)));
    }
    AggregatePrevote(request) {
        const data = exports.QueryAggregatePrevote.encode(request).finish();
        const promise = this.rpc.request(this.service, "AggregatePrevote", data);
        return promise.then((data) => exports.QueryAggregatePrevoteResponse.decode(new wire_1.BinaryReader(data)));
    }
    AggregatePrevotes(request) {
        const data = exports.QueryAggregatePrevotes.encode(request).finish();
        const promise = this.rpc.request(this.service, "AggregatePrevotes", data);
        return promise.then((data) => exports.QueryAggregatePrevotesResponse.decode(new wire_1.BinaryReader(data)));
    }
    AggregateVote(request) {
        const data = exports.QueryAggregateVote.encode(request).finish();
        const promise = this.rpc.request(this.service, "AggregateVote", data);
        return promise.then((data) => exports.QueryAggregateVoteResponse.decode(new wire_1.BinaryReader(data)));
    }
    AggregateVotes(request) {
        const data = exports.QueryAggregateVotes.encode(request).finish();
        const promise = this.rpc.request(this.service, "AggregateVotes", data);
        return promise.then((data) => exports.QueryAggregateVotesResponse.decode(new wire_1.BinaryReader(data)));
    }
    Params(request) {
        const data = exports.QueryParams.encode(request).finish();
        const promise = this.rpc.request(this.service, "Params", data);
        return promise.then((data) => exports.QueryParamsResponse.decode(new wire_1.BinaryReader(data)));
    }
    Medians(request) {
        const data = exports.QueryMedians.encode(request).finish();
        const promise = this.rpc.request(this.service, "Medians", data);
        return promise.then((data) => exports.QueryMediansResponse.decode(new wire_1.BinaryReader(data)));
    }
    MedianDeviations(request) {
        const data = exports.QueryMedianDeviations.encode(request).finish();
        const promise = this.rpc.request(this.service, "MedianDeviations", data);
        return promise.then((data) => exports.QueryMedianDeviationsResponse.decode(new wire_1.BinaryReader(data)));
    }
    ValidatorRewardSet(request) {
        const data = exports.QueryValidatorRewardSet.encode(request).finish();
        const promise = this.rpc.request(this.service, "ValidatorRewardSet", data);
        return promise.then((data) => exports.QueryValidatorRewardSetResponse.decode(new wire_1.BinaryReader(data)));
    }
    EMA(request) {
        const data = exports.QueryEMARequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "EMA", data);
        return promise.then((data) => exports.QueryEMAResponse.decode(new wire_1.BinaryReader(data)));
    }
    WMA(request) {
        const data = exports.QueryWMARequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "WMA", data);
        return promise.then((data) => exports.QueryWMAResponse.decode(new wire_1.BinaryReader(data)));
    }
    SMA(request) {
        const data = exports.QuerySMARequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "SMA", data);
        return promise.then((data) => exports.QuerySMAResponse.decode(new wire_1.BinaryReader(data)));
    }
    ConvertUSDCtoCHEQ(request) {
        const data = exports.ConvertUSDCtoCHEQRequest.encode(request).finish();
        const promise = this.rpc.request(this.service, "ConvertUSDCtoCHEQ", data);
        return promise.then((data) => exports.ConvertUSDCtoCHEQResponse.decode(new wire_1.BinaryReader(data)));
    }
}
exports.QueryClientImpl = QueryClientImpl;
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=query.js.map